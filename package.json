{
  "name": "learn-rights-monorepo",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build:web": "cd frontend && npm run build:web",
    "vercel-build": "cd frontend && npm run vercel-build"
  }
}
